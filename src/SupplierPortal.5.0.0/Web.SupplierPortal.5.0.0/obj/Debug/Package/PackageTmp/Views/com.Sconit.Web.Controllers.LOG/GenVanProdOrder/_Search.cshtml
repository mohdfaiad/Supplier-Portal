@using com.Sconit.Web.Models.SearchModels.LOG
<fieldset>
    @using (Html.BeginForm("List", "GenVanProdOrder"))
    {   
        <div class="search">
            <div class="search-label">
                <label for="GenVanProdOrder_ZLINE">
                    @Resources.LOG.GenVanProdOrder.GenVanProdOrder_ZLINE
                </label>
            </div>
            <div class="search-field">
                @Html.TextBox("SearchZLINE", TempData["GenVanProdOrderSearchModel"] != null ? ((GenVanProdOrderSearchModel)TempData["GenVanProdOrderSearchModel"]).SearchZLINE : string.Empty)
            </div>
        </div>
        <div class="search-footer">
            @Html.Hidden("isFromList", true)
            <button type="submit" id="btnSearch">
                @Resources.Global.Button_Search</button>
        </div>
    }
</fieldset>
