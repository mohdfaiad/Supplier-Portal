@model IEnumerable<com.Sconit.Entity.SCM.FlowDetail>
<fieldset>
    @(Html.Telerik().Grid(Model)
        .Name("ItemFlowGrid")
        .DataKeys(keys =>
        {
            keys.Add(p => p.Id);
        })
        .Columns(columns =>
        {
            columns.Bound(o => o.Flow);
            columns.Bound(o => o.FlowDescription);
            columns.Bound(o => o.PartyFrom);
            columns.Bound(o => o.PartyTo);
            columns.Bound(o => o.LocationFrom);
            columns.Bound(o => o.LocationTo);
            columns.Bound(o => o.UnitCountDescription);
            columns.Bound(o => o.Item);
            columns.Bound(o => o.ReferenceItemCode);
            columns.Bound(o => o.ItemDescription);
            columns.Bound(o => o.MinUnitCount);
            columns.Bound(o => o.ErrorType);
            columns.Bound(o => o.ErrorTypeDesc).Sortable(false);
        })

        //.Scrollable(c => c.Enabled(false))
        .EnableCustomBinding(true)
                .DataBinding(dataBinding => dataBinding.Ajax().Select("_AjaxCheckFlowList", "ItemFlow"))

        .Pageable(settings =>
        {
            settings.Total(ViewBag.Total != null ? ViewBag.Total : 0);
            settings.PageSize(ViewBag.PageSize);
        })
        .Resizable(resizing => resizing.Columns(true))
    )
</fieldset>
