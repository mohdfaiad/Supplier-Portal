@using com.Sconit.Web.Models.SearchModels
@model IEnumerable<com.Sconit.Entity.ORD.CabProductionView>
<fieldset>
    <legend>RAC00</legend>
    @(Html.Telerik().Grid(Model)
                  .Name("OutSoureOutGrid1")
          .DataKeys(keys =>
                        {
                            keys.Add(p => p.OrderDetailId);
                        })
          .Columns(columns =>
          {
              columns.Bound(o => o.Sequence);
              columns.Bound(o => o.SubSequence);
              columns.Bound(o => o.TraceCode);
              columns.Bound(o => o.Model);
              columns.Bound(o => o.ModelDescription);
              columns.Bound(o => o.Item);
              columns.Bound(o => o.ItemDesc);
              columns.Bound(o => o.StartTime);
              columns.Bound(o => o.Flow);
              columns.Bound(o => o.CabOutStatusDescription);
          })
          .Scrollable(c => c.Enabled(false))
                                                                  .DataBinding(dataBinding => dataBinding.Ajax().Select("_AjaxOutSoureViewList", "CabGuide", new RouteValueDictionary { { "Type", (int)com.Sconit.CodeMaster.CabType.Purchase }, { "IsOut", "true" }, { "Flow", "RAC00" } }))
          .Pageable(settings =>
                        {
                            settings.Total(ViewBag.Total != null ? ViewBag.Total : 0);
                            settings.PageSize(ViewBag.PageSize);
                        })
                        .EnableCustomBinding(true)
          .Sortable()
          .Resizable(resizing => resizing.Columns(true)))
</fieldset>
<fieldset>
    <legend>RAD00</legend>
    @(Html.Telerik().Grid(Model)
                  .Name("OutSoureOutGrid2")
          .DataKeys(keys =>
                        {
                            keys.Add(p => p.OrderDetailId);
                        })
          .Columns(columns =>
          {
              columns.Bound(o => o.Sequence);
              columns.Bound(o => o.SubSequence);
              columns.Bound(o => o.TraceCode);
              columns.Bound(o => o.Model);
              columns.Bound(o => o.ModelDescription);
              columns.Bound(o => o.Item);
              columns.Bound(o => o.ItemDesc);
              columns.Bound(o => o.StartTime);
              columns.Bound(o => o.Flow);
              columns.Bound(o => o.CabOutStatusDescription);
          })
          .Scrollable(c => c.Enabled(false))
                                                                  .DataBinding(dataBinding => dataBinding.Ajax().Select("_AjaxOutSoureViewList", "CabGuide", new RouteValueDictionary { { "Type", (int)com.Sconit.CodeMaster.CabType.Purchase }, { "IsOut", "true" }, { "Flow", "RAD00" } }))
          .Pageable(settings =>
                        {
                            settings.Total(ViewBag.Total != null ? ViewBag.Total : 0);
                            settings.PageSize(ViewBag.PageSize);
                        })
                        .EnableCustomBinding(true)
          .Sortable()
          .Resizable(resizing => resizing.Columns(true)))
</fieldset>
